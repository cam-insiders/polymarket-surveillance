"""
Visualization helpers for experiment outputs (domain matrix bar charts, category eval
bar charts, sweep dashboards, etc.).

Run chart CLIs as modules, for example:

    python -m experiments.charting.domain_matrix_charts --list
    python -m experiments.charting.category_eval_charts --list
    python -m experiments.charting.sweep_flag_rate_charts --list
    python -m experiments.charting.sweep_min_usd_charts --list
    python -m experiments.charting.curated_compare_sota_charts --list
"""
